enum SizeEnum {
  ExtraSmall = "xs",
  Small = "sm",
  Medium = "md",
  Large = "lg",
  ExtraLarge = "xl",
};

export const size = ({ size: SizeEnum }) => {
  switch (size) {
    case SizeEnum.ExtraSmall:
      
    default:
      return;

  }
};
